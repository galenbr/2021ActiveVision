<launch>

  <!-- This file makes it easy to include the settings for sensor managers -->
<rosparam command="load" file="$(find panda_moveit)/config/sensors_panda.yaml" />

<!-- launch the octomap server -->
 <node pkg="octomap_server" type="octomap_server_node" name="octomap_server">
    <param name="resolution" value="0.05" />

    <!-- fixed map frame (set to 'map' if SLAM or localization running!) default 'odom_combined'-->
    <param name="frame_id" type="string" value="camera_link" />

    <!-- maximum range to integrate (speedup!) -->
    <param name="sensor_model/max_range" value="5.0" />

    <!-- data source to integrate (PointCloud2) -->
    <remap from="cloud_in" to="/panda_camera/depth/points" />

  </node>

  <!-- Load the robot specific sensor manager; this sets the moveit_sensor_manager ROS parameter -->
  <arg name="moveit_sensor_manager" default="panda" />
  <include file="$(find panda_moveit)/launch/$(arg moveit_sensor_manager)_moveit_sensor_manager.launch.xml" />

</launch>
